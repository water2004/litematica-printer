plugins {
    id 'java-gradle-plugin'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

gradlePlugin {
    plugins {
        modPlugin {
            id = 'mod-plugin'
            implementationClass = 'ModPlugin'
        }
    }
}
